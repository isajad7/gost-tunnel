<div align="center">

# 🚀 Secure GOST Tunnel Automation
### اسکریپت هوشمند و خودکار تانلینگ امن (GOST) با پروتکل WSS
**بهترین راهکار برای عبور از محدودیت‌های شبکه و فیلترینگ بین سرور ایران و خارج**

[![GitHub stars](https://img.shields.io/github/stars/isajad7/gost-tunnel?style=social)](https://github.com/isajad7/gost-tunnel/stargazers)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Bash](https://img.shields.io/badge/Language-Bash-green.svg)]()

<p align="center">
  <a href="#نصب-سریع">نصب سریع</a> •
  <a href="#مرحله-۱-تنظیم-سرور-خارج">سرور خارج</a> •
  <a href="#مرحله-۲-تنظیم-سرور-ایران">سرور ایران</a> •
  <a href="#مانیتورینگ-و-عیب‌یابی">عیب‌یابی</a>
</p>

</div>

---

## 📖 درباره پروژه
این اسکریپت فرآیند پیچیده‌ی راه‌اندازی تانل **GOST** را به صورت کاملاً خودکار انجام می‌دهد. با استفاده از این ابزار، می‌توانید ترافیک را از سرور ایران دریافت کرده و از طریق یک تانل رمزنگاری شده (WebSocket Secure) به سرور خارج منتقل کنید.

### ✨ چرا این اسکریپت؟
- **فیکس باگ IPv6:** حل مشکل عدم اتصال در برخی دیتاسنترهای ایران با اجبار روی IPv4.
- **مولتی پورت:** انتقال همزمان چندین پورت (مثلاً 443, 2095, 2053) تنها با یک تانل.
- **پایداری:** ساخت سرویس `Systemd` برای اجرای دائم و خودکار پس از ریستارت سرور.
- **امنیت:** استفاده از یوزرنیم و پسورد برای جلوگیری از استفاده غیرمجاز از تانل شما.

---

## ⚡ نصب سریع (Quick Install)

در **هر دو سرور** (هم ایران و هم خارج)، با دسترسی روت (`sudo -i`) دستور زیر را اجرا کنید:

```bash
bash <(curl -Ls [https://raw.githubusercontent.com/isajad7/gost-tunnel/main/install.sh](https://raw.githubusercontent.com/isajad7/gost-tunnel/main/install.sh))


📚 راهنمای قدم‌به‌قدم (Step-by-Step Guide)
برای اینکه تانل به درستی کار کند، باید مراحل را دقیقاً به ترتیب زیر انجام دهید.

مرحله ۱: تنظیم سرور خارج (Foreign Server) 🌍
ابتدا باید مقصد را آماده کنیم.

به سرور خارج SSH بزنید.

دستور نصب بالا را اجرا کنید.

از منوی نمایش داده شده، گزینه 1 (Configure as FOREIGN Server) را انتخاب کنید.

پورت تانل: یک پورت برای برقراری ارتباط تانل وارد کنید (پیش‌فرض 2083 پیشنهاد می‌شود).

نام کاربری و رمز: یک یوزرنیم و پسورد دلخواه وارد کنید (این‌ها را یادداشت کنید، در سرور ایران نیاز دارید).

تمام! سرور خارج اکنون منتظر اتصال است.

نکته: مطمئن شوید پنل وی‌پی‌ان شما (مثل سنایی یا Xray) در سرور خارج روشن است و روی پورت‌های مقصد (مثلاً 2095) در حال سرویس‌دهی است.

مرحله ۲: تنظیم سرور ایران (Iran Server) 🇮🇷
حالا باید مبدا را به مقصد وصل کنیم.

به سرور ایران SSH بزنید.

دستور نصب بالا را اجرا کنید.

از منوی نمایش داده شده، گزینه 2 (Configure as IRAN Server) را انتخاب کنید.

اسکریپت از شما اطلاعات زیر را می‌پرسد:

IP سرور خارج: آی‌پی عددی سرور خارج خود را وارد کنید.

پورت تانل: همان پورتی که در مرحله قبل در سرور خارج وارد کردید (مثلاً 2083).

یوزرنیم و پسورد: همان اطلاعاتی که در سرور خارج ست کردید.

انتخاب پورت‌های انتقال:

حالا لیست پورت‌هایی که می‌خواهید کاربران به آن وصل شوند را با فاصله (Space) وارد کنید.

مثال: اگر پنل شما روی پورت 2095 است و یک کانفیگ هم روی 443 دارید، بنویسید:

2095 443

تمام! تانل برقرار شد.

📊 مانیتورینگ و عیب‌یابی
چطور مطمئن شوم تانل وصل است؟
در هر کدام از سرورها، دوباره دستور نصب را اجرا کنید و گزینه 3 (Check Tunnel Logs) را انتخاب کنید.

اگر لاگ‌هایی به شکل <-> دیدید، یعنی ترافیک با موفقیت در حال عبور است.

اگر لاگ Connect دیدید اما اینترنت نداشتید، پورت‌های پنل مقصد را چک کنید.

ارور 503 Service Unavailable
این ارور یعنی تانل تا سرور خارج رفته، اما آنجا بسته شده است.

چک کنید در سرور خارج، سرویس gost-tunnel روشن باشد.

چک کنید پورتی که فوروارد می‌کنید (مثلاً 2095) در سرور خارج واقعاً توسط پنل Xray اشغال شده باشد.

حذف اسکریپت
اگر خواستید تانل را کامل پاک کنید، کافیست گزینه 4 را از منو انتخاب کنید.

<div align="center"> <sub>Developed & Optimized for Freedom by <b>Sajjad</b></sub> </div>


### تغییرات و بهبودها در این نسخه:

1.  **لینک مستقیم شما:** لینک `raw.githubusercontent.com/isajad7/gost-tunnel` در بخش نصب قرار گرفت.
2.  **دستورالعمل خط به خط:** دقیقاً مشخص کردم اول کدام سرور و بعد کدام سرور کانفیگ شود.
3.  **مثال‌های واقعی:** برای بخش پورت‌ها مثال زدم (`2095 443`) تا کاربر گیج نشود.
4.  **بخش عیب‌یابی:** ارور رایج 503 که خودمان با آن مواجه شدیم را توضیح دادم.
5.  **ظاهر گرافیکی:** استفاده از ایموجی پرچم‌ها و آیکون‌ها برای خوانایی بهتر.